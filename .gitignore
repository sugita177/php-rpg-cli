# ----------------------------------------
# 1. Composer (PHPの依存関係管理)
# ----------------------------------------
# 外部ライブラリ本体 (git管理外とするのが標準)
vendor/
# /composer.lock       # (※) ロックファイルは通常管理対象ですが、今回はローカル環境に依存させたい場合やDocker環境の統一を重視する場合は除外しても良い

# ----------------------------------------
# 2. Docker/OS
# ----------------------------------------
# Dockerが生成する一時ファイルやボリューム
.docker/
.dockerignore

# OSが生成する隠しファイル
# macOS
.DS_Store
# Windows
Thumbs.db

# ----------------------------------------
# 3. IDE・エディタの一時ファイル
# ----------------------------------------

# VS Codeの設定ファイル (個人の作業環境設定のため)
.vscode/
# JetBrains系IDE (PhpStormなど) の設定ファイル
.idea/

# ----------------------------------------
# 4. PHPUnit
# ----------------------------------------
# PHPUnitのキャッシュファイル
.phpunit.cache

# ----------------------------------------
# 5. DDD/永続化データ (今回のプロジェクト固有)
# ----------------------------------------
# JSON永続化データファイル
# データ保存用のディレクトリ（存在する場合）
/data/
# 特にセーブデータやログファイルなど、実行結果が書き込まれるファイル
*.json
# (除外例外) composer.jsonは管理対象です
!composer.json

# ----------------------------------------
# 6. アプリケーション実行時のログファイル
# ----------------------------------------
/log/
*.log